# This file is managed by Ansible, do not edit directly

# Replace %h by %a to print real-ip if behind a proxy, and mod_remoteip configured
LogFormat "%a %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\"" combined
